import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ComingSoon';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: '',
  },
});
